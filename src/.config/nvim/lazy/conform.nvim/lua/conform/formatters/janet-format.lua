--@type conform.FileFormatterConfig
return {
  meta = {
    url = "BAD_URL_github.com/janet-lang/spork",
    description = "A formatter for Janet code.",
  },
  command = "janet-format",
  stdin = true,
}
