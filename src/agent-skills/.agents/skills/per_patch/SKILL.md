---
name: per_patch
description: Ensure instructions are done per patch (jj patch, stg, git patch etc).
---

# per_patch

Make sure instructions are done per patch (e.g. jj patch, old stg command or git patch).

## Trigger

User writes `/per_patch`.

## Procedure

1. Group changes per logical patch.
2. Create/commit per patch, not one big patch.
